<fragment language="java" name="logging_fields">
  <dependencies>
    <required name="logging_dependencies"/>
  </dependencies>

  <code black-list="imports, if, for">
    private static final Logger iLogger = Logger.getLogger(getClass().getName());
    private static final Handler iHandler = newConsoleHandler();
  </code>
</fragment>
