<fragment language="java" name="logging_init">
  <dependencies>
    <required name="logging_imports"/>
    <required name="logging_fields"/>
  </dependencies>

  <code black-list="imports, if, for">
    iHandler.setLevel(Level.ALL);
    iLogger.addHandler(iHandler);
    iLogger.setLevel(Level.ALL);
  </code>
</fragment>
